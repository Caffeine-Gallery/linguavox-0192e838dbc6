service : {
  addTranslation: (text, text, text) -> ();
  getHistory: () -> (vec record {
                           text;
                           text;
                           text;
                           int;
                         }) query;
}
